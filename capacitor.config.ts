import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final2_FranciscoSalinas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
